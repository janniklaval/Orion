"
A ORActionMoveFAMIXAnnotationType is a generated class useful to Orion. It corresponds to an action that can be performed on a type of model element 
"
Class {
	#name : #ORActionMoveAnnotationType,
	#superclass : #ORActionMove,
	#category : #'OrionFamixCore-Action'
}

{ #category : #'method execution' }
ORActionMoveAnnotationType >> run [
	self shouldBeImplemented
]
